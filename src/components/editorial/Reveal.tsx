// Promote the landing's scroll-reveal primitives to the app-wide editorial DS.
// Same behaviour (IntersectionObserver + `.lp-reveal` CSS), neutral import path.
export { Reveal } from "@/components/landing/Reveal";
export { useReveal } from "@/components/landing/useReveal";
